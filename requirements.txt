# Telegram bot
python-telegram-bot>=22.0   # 21.x breaks on Python 3.14's stricter get_event_loop()
python-dotenv>=1.0.0

# LLM authoring (Groq Llama by default; OpenAI/Anthropic optional)
groq>=0.11.0

# Transcription / video pipeline
yt-dlp>=2025.1.0

# PDF / image processing
reportlab>=4.0.0
pymupdf>=1.24.0
pypdf>=4.0.0
imagehash>=4.3.0
Pillow>=10.0.0

# Web API (Phase 0 SaaS frontend)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Phase 1: Auth + DB
SQLAlchemy[asyncio]>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0       # async Postgres driver (used by FastAPI handlers)
aiosqlite>=0.20.0     # async SQLite driver (used in local dev)
psycopg[binary]>=3.1.0  # sync Postgres driver (used by the worker thread)
greenlet>=3.0.0       # required by SQLAlchemy async

# Phase 2: Razorpay payments (UPI top-up wallet)
razorpay>=1.4.2

# Self-hosted Whisper (no rate limits) — used when WHISPER_BACKEND=local.
# Brings in ctranslate2, tokenizers, onnxruntime — ~200 MB install.
faster-whisper>=1.0.3
